\documentclass{beerydoc}


\usepackage{clockdate}
\clockdatesetup{overwrite-date}
\ExplSyntaxOn
\clockdate_clock_keys_save:nn { or }
  {
      clocks-pair-separator  = {  ~or~ }
    , clocks-final-separator = { ,~or~ }
  }
\clockdate_date_keys_save:nn { or }
  {
      days-pair-separator    = {  ~or~ }
    , days-final-separator   = { ,~or~ }
    , months-pair-separator  = {  ~or~ }
    , months-final-separator = { ,~or~ }
  }
\ExplSyntaxOff


\begin{document}


\section*{\LARGE The \pkg{clockdate} package}

Print the time of day and the calendar date

Oliver Beery

Version 1.0.0\quad\date{2026-9-20}


\section{Introduction}
\label{sec:intro}

\subsection{About}
\label{subsec:about}

This package provides \cs{clock} and \cs{calendardate}.
\cs{clock} prints the time of day.
\cs{calendardate} prints the calendar date.
These commands are ideal for text markup because their syntax is short,
flexible, easy-to-understand, and unambiguous.%
\footnote
  {%
    Not entirely unambiguous. See the key \key{date/priority}
    (\S\ref{subsec:date}).%
  }
The printed time of day and calendar date are highly customizable through a
variety of key-value options.
For advanced users, this package also provides robust \LaTeX3 programming
support.

\subsection{Loading the package}
\label{subsec:load}

\listheading{Requirements:}
\begin{itemize}
  \item \LaTeXe{} version 2023-11-01 or newer
  \item \pkg{l3kernel} version 2023-11-01 or newer
\end{itemize}

\subsection{Syntax}
\label{subsec:syntax}

This documentation uses the syntax \meta{integer}.
This syntax has the same meaning as the argument to \cs{inteval}, which is
documented in \pkg{usrguide}.


\section{Commands}
\label{sec:cmds}

This section documents the commands provided by the \pkg{clockdate} package.

\begin{code}{\clockdatesetup}
  \begin{syntax}
    \cs{clockdatesetup} \marg{key-value list}
  \end{syntax}
  Sets and processes the \pkg{clockdate} package keys (\S\ref{sec:keys}) in
  \meta{key-value list}.
  All assignments made by \cs{clockdatesetup} are local to the current group.
  Can be used mid-document.
\end{code}

\begin{code}{\clock}
  \begin{syntax}
    \cs{clock} \oarg{key-value list} \marg{clock(s)}
  \end{syntax}
  Prints the time of day specified by \meta{clock(s)}.
  \meta{clock(s)} must take any of the following forms:
  \begin{itemize}
    \item a single clock \meta{clock}
    \item a clock range \meta{clock_1}|--|\meta{clock_2}
    \item
    a comma-separated list where each item is a single clock or a clock range
  \end{itemize}

  \meta{clock} must take any of the following forms:
  \begin{itemize}
    \item \meta{hour}
    \item \meta{hour}|:|\meta{minute}
    \item \meta{hour}|:|\meta{minute}|:|\meta{second}
  \end{itemize}

  \meta{hour} must be an \meta{integer} from \num{0} to \num{24}.
  \meta{minute} must be an \meta{integer} from \num{0} to \num{59}.
  \meta{second} must be an \meta{integer} from \num{0} to \num{60}.
  (The value of \num{60} allows for leap seconds.)
  If $\meta{hour}=\num{24}$, then \meta{minute} and \meta{second} must be zero.

  The optional argument sets the keys in \meta{key-value list} in path |clock|
  for only that instance of \cs{clock}.

  Additional parsing details for \meta{clock(s)}:
  \begin{itemize}
    \item
    \meta{hour}, \meta{minute}, and \meta{second} may be enclosed in braces.
    This prevents the parser from seeing the enclosed |--|.
    (|--| is valid input within an integer expression.)
    \item
    Colons of catcode 11 and 13 (active) at the top level are replaced by that
    of catcode 12.
    \item Active hyphens at the top level are replaced by that of catcode 12.
  \end{itemize}
\end{code}

\begin{code}{\calendardate, \Calendardate}
  \begin{syntax}
    \cs{calendardate} \oarg{key-value list} \marg{date(s)}
  \end{syntax}
  Prints the calendar date specified by \meta{date(s)}.
  \meta{date(s)} must take any of the following forms:
  \begin{itemize}
    \item \meta{year}|-|\meta{month}|-|\meta{day(s)}
    \item \meta{month}|-|\meta{day(s)}
    \item \meta{year}|-|\meta{month(s)}
    \item \meta{year}
  \end{itemize}

  \meta{year} may be substituted with \meta{empty}|-|\meta{year} to ensure that
  the year is always before the common era.

  \meta{day(s)} must take any of the following forms:
  \begin{itemize}
    \item a single day \meta{day}
    \item a day range \meta{day_1}|--|\meta{day_2}
    \item a comma-separated list where each item is a single day or a day range
  \end{itemize}

  \meta{month(s)} must take any of the following forms:
  \begin{itemize}
    \item a single month \meta{month}
    \item a month range \meta{month_1}|--|\meta{month_2}
    \item
    a comma-separated list where each item is a single month or a month range
  \end{itemize}

  \meta{year} and \meta{day} must be an \meta{integer}.
  \meta{month} must be an \meta{integer} from \num{1} to \num{12}.
  Each calendar date must be valid.
  \date{2-29} is always valid if \meta{year} is omitted.
  If \meta{year} is provided, \date{2-29} is error-checked using the Julian
  calendar.

  The optional argument sets the keys in \meta{key-value list} in path |date|
  for only that instance of \cs{calendardate}.

  \cs{Calendardate} is equivalent to \cs{calendardate}, except that it sets the
  key \key{date/capitalize-first=true} (\S\ref{subsec:date}) for only that
  instance of \cs{Calendardate}.
  (The value of the key \key{date/capitalize-first} may be overwritten by
  \meta{key-value list}.)

  Additional parsing details for \meta{date(s)}:
  \begin{itemize}
    \item |-| and |--| at the top level must only be used as a delimiter.
    \item
    \meta{year}, \meta{month}, and \meta{day} may be enclosed in braces.
    This prevents the parser from seeing the enclosed |-| or |--|.
    (|--| is valid input within an integer expression.)
    \item Active hyphens at the top level are replaced by that of catcode 12.
  \end{itemize}
\end{code}


\section{Keys}
\label{sec:keys}

This section documents the keys provided by the \pkg{clockdate} package.
Set the package keys using \cs{clockdatesetup} (\S\ref{sec:cmds}).

\begin{code}{overwrite-date, \date, \Date}
  Redefines \cs{date} as equivalent to \cs{calendardate}.
  Defines \cs{Date} as equivalent to \cs{Calendardate}.
  This is done at the \hook{begindocument} hook so that the original
  definition of \cs{date} remains usuable in the preamble.
  This key must be used in the preamble.
\end{code}

\begin{code}{reset}
  Meta key that sets the keys \key{clock/reset} and \key{date/reset}.
\end{code}

\subsection{\cs{clock}}
\label{subsec:clock}

This subsection documents the keys in path |clock|.
These keys modify the behavior of \cs{clock} (\S\ref{sec:cmds}).

\begin{code}{clock/reset}
  Meta key that sets all the keys in path |clock| to their initial values.
\end{code}

\begin{code}{clock/load}
  \begin{syntax}
    \key{clock/load=\meta{string}}
  \end{syntax}
  Used to load precompiled keys in path |clock|.
  See \S\ref{subsec:save} for more detail.
\end{code}

\begin{code}{clock/clock}
  \begin{syntax}
    \key{clock/clock=12-hour|24-hour}
  \end{syntax}
  Choice key that sets the format in which the clock is printed.
  If the value is |24-hour|, the am/pm is always omitted.
  The initial value is |12-hour|.
\end{code}

\subsubsection{Printing the hour, minute, and second}
\label{subsubsec:printhms}

\begin{code}{clock/hour}
  \begin{syntax}
    \key{clock/hour=h|hh}
  \end{syntax}
  Choice key that sets the format in which the hour is printed.
  |h| prints the hour with the minimum number of digits.
  |hh| prints the hour with two digits, adding a leading zero if necessary.
  The initial value is |h|.
\end{code}

\begin{code}{clock/minute}
  \begin{syntax}
    \key{clock/minute=m|mm}
  \end{syntax}
  Choice key that sets the format in which the minute is printed.
  |m| prints the minute with the minimum number of digits.
  |mm| prints the minute with two digits, adding a leading zero if necessary.
  The initial value is |mm|.
\end{code}

\begin{code}{clock/second}
  \begin{syntax}
    \key{clock/second=s|ss}
  \end{syntax}
  Choice key that sets the format in which the second is printed.
  |s| prints the second with the minimum number of digits.
  |ss| prints the second with two digits, adding a leading zero if necessary.
  The initial value is |ss|.
\end{code}

\subsubsection{Separating the hour, minute, and second}
\label{subsubsec:sephms}

\begin{code}{clock/hour-minute-separator}
  \begin{syntax}
    \key{clock/hour-minute-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{hour} and \meta{minute} to
  \meta{token list}.
  The initial value is |:|.
\end{code}

\begin{code}{clock/minute-second-separator}
  \begin{syntax}
    \key{clock/minute-second-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{minute} and \meta{second} to
  \meta{token list}.
  The initial value is |:|.
\end{code}

\begin{code}{clock/time-separator}
  \begin{syntax}
    \key{clock/time-separator=\meta{token list}}
  \end{syntax}
  Meta key that sets the keys \key{clock/hour-minute-separator} and
  \key{clock/minute-second-separator} to \meta{token list}.
\end{code}

\subsubsection{Printing the am/pm}
\label{subsubsec:printampm}

\begin{code}{clock/ampm}
  \begin{syntax}
    \key{clock/ampm=a.m.|am|a|A.M.|AM|A|none}
  \end{syntax}
  Choice key that sets the format in which the am/pm is printed.
  The initial value is |AM|.
  \input{clockdate-input-ampm.tex}

  The am/pm is omitted if \key{clock/ampm=none} or \key{clock/clock=24-hour}.

  If the value is |a.m.| or |A.M.|, the trailing am/pm will not print an extra
  period if the next token after \cs{clock} is a period.
  If the am/pm prints the period, it is followed by \cs{@} because the sentence
  does not end here.
\end{code}

\begin{code}{clock/ampm-separator}
  \begin{syntax}
    \key{clock/ampm-separator=\meta{token list}}
  \end{syntax}
  Sets the separator before the am/pm to \meta{token list}.
  The initial value is \cs{nobreakspace}.
\end{code}

\begin{code}{clock/ampm-omit-range}
  \begin{syntax}
    \key{clock/ampm-omit-range=true|false}
  \end{syntax}
  Boolean key that sets if \meta{clock_1} omits the am/pm in a clock range if
  \meta{clock_1} and \meta{clock_2} would print the same meridiem and
  $\meta{clock_2}>\meta{clock_1}$.
  The initial value is |true|.
\end{code}

\begin{code}{clock/ampm-omit-list}
  \begin{syntax}
    \key{clock/ampm-omit-list=true|false}
  \end{syntax}
  Boolean key that sets if each \meta{clock}---except the very last---omits the
  am/pm if each \meta{clock} would print the same meridiem.
  The initial value is |true|.
\end{code}

\subsubsection{Separating the clocks}
\label{subsubsec:sepclocks}

\begin{code}{clock/clocks-separator}
  \begin{syntax}
    \key{clock/clocks-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{clocks}---except the last
  two---to \meta{token list} if \meta{clocks} contains three or more items.
  The initial value is |, |.
\end{code}

\begin{code}{clock/clocks-pair-separator}
  \begin{syntax}
    \key{clock/clocks-pair-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{clocks} to \meta{token list} if
  \meta{clocks} contains exactly two items.
  The initial value is | and |.
\end{code}

\begin{code}{clock/clocks-final-separator}
  \begin{syntax}
    \key{clock/clocks-final-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between the last two items in \meta{clocks} to
  \meta{token list} if \meta{clocks} contains three or more items.
  The initial value is |, and |.
\end{code}

\begin{code}{clock/clocks-range-separator}
  \begin{syntax}
    \key{clock/clocks-range-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{clock_1} and \meta{clock_2} in a clock range
  to \meta{token list}.
  The initial value is | to |.
\end{code}

\subsection{\cs{calendardate}}
\label{subsec:date}

This subsection documents the keys in path |date|.
These keys modify the behavior of \cs{calendardate} (\S\ref{sec:cmds}).

\begin{code}{date/reset}
  Meta key that sets all the keys in path |date| to their initial values.
\end{code}

\begin{code}{date/load}
  \begin{syntax}
    \key{date/load=\meta{string}}
  \end{syntax}
  Used to load precompiled keys in path |date|.
  See \S\ref{subsec:save} for more detail.
\end{code}

\begin{code}{date/priority}
  \begin{syntax}
    \key{date/priority=MD|YM}
  \end{syntax}
  Choice key that sets the typeset representation if the parser cannot
  distinguish between \meta{month}|-|\meta{day(s)} and
  \meta{year}|-|\meta{month(s)}.
  This situation occurs if \meta{date(s)} contains exactly two fields and all
  values within are an \meta{integer} from \num{1} to \num{12}.
  The initial value is |MD|.
\end{code}

\begin{code}{date/order}
  \begin{syntax}
    \key{date/order=DMY|YMD|MDY}
  \end{syntax}
  Choice key that sets the order in which the year, month, and day are printed.
  The initial value is |DMY|.
\end{code}

\begin{code}{date/capitalize-first}
  \begin{syntax}
    \key{date/capitalize-first=true|false}
  \end{syntax}
  Boolean key that sets if the month will be capitalized if it is printed at
  the start of \cs{calendardate}.
  See \cs{Calendardate} (\S\ref{sec:cmds}).
  The initial value is |false|.
  See \S\ref{subsec:expl3date} for more details.
\end{code}

\subsubsection{Printing the year, month, and day}
\label{subsubsec:printymd}

\begin{code}{date/year}
  \begin{syntax}
    \key{date/year=y|yy|yyyy}
  \end{syntax}
  Choice key that sets the format in which the year is printed.
  |y| prints the year with the minimum number of digits.
  |yy| prints the year with only the last two digits, adding a leading zero if
  the year is a single digit.
  |yyyy| prints the year with a minimum of four digits, adding leading zeroes
  if necessary.
  The initial value is |y|.
\end{code}

\begin{code}{date/month}
  \begin{syntax}
    \key{date/month=m|mm|mmm|mmmm}
  \end{syntax}
  Choice key that sets the format in which the month is printed.
  |m| prints the month with the minimum number of digits.
  |mm| prints the month with two digits, adding a leading zero if necessary.
  |mmm| prints the month in short form.
  |mmmm| prints the month with the name of the month fully spelled out.
  The initial value is |mmmm|.

  By default, |mmm| prints the month in the same form as |mmmm|, except that
  some months are abbreviated: Jan., Feb., Aug., Sept., Oct., Nov., Dec.
  The abbreviated month will not print an extra period if the next token after
  \cs{calendardate} is a period and \cs{calendardate} ends with an abbreviated
  month that ends in a period.
  If the abbreviated month prints the period, it is followed by \cs{@} because
  the sentence does not end here.
  See \S\ref{subsec:expl3date} for more details.
\end{code}

\begin{code}{date/day}
  \begin{syntax}
    \key{date/day=d|dd}
  \end{syntax}
  Choice key that sets the format in which the day is printed.
  |d| prints the day with the minimum number of digits.
  |dd| prints the day with two digits, adding a leading zero if necessary.
  The initial value is |d|.
\end{code}

\begin{code}{date/day-ordinal}
  \begin{syntax}
    \key{date/day-ordinal=true|false}
  \end{syntax}
  Boolean key that sets if the day is printed as an ordinal day (e.g.
  1\textsuperscript{st}, 4\textsuperscript{th}).
  The initial value is |false|.
  See \S\ref{subsec:expl3date} to customize the display of the ordinal day.
\end{code}

\subsubsection{Separating the year, month, and day}
\label{subsubsec:sepymd}

\begin{code}{date/year-month-separator}
  \begin{syntax}
    \key{date/year-month-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{year} and \meta{month} to \meta{token list}.
  The initial value is | |.
\end{code}

\begin{code}{date/month-day-separator}
  \begin{syntax}
    \key{date/month-day-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{month} and \meta{day} to \meta{token list}.
  The initial value is | |.
\end{code}

\begin{code}{date/year-day-separator}
  \begin{syntax}
    \key{date/year-day-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{year} and \meta{day} to \meta{token list}.
  The initial value is |, |.
\end{code}

\begin{code}{date/date-separator}
  \begin{syntax}
    \key{date/date-separator=\meta{token list}}
  \end{syntax}
  Meta key that sets the following keys to \meta{token list}:
  \begin{itemize}
    \item \key{date/year-month-separator}
    \item \key{date/month-day-separator}
    \item \key{date/year-day-separator}
  \end{itemize}
\end{code}

\subsubsection{Separating the days}
\label{subsubsec:sepdays}

\begin{code}{date/days-separator}
  \begin{syntax}
    \key{date/days-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{days}---except the last
  two---to \meta{token list} if \meta{days} contains three or more items.
  The initial value is |, |.
\end{code}

\begin{code}{date/days-pair-separator}
  \begin{syntax}
    \key{date/days-pair-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{days} to \meta{token list} if
  \meta{days} contains exactly two items.
  The initial value is | and |.
\end{code}

\begin{code}{date/days-final-separator}
  \begin{syntax}
    \key{date/days-final-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between the last two items in \meta{days} to
  \meta{token list} if \meta{days} contains three or more items.
  The initial value is |, and |.
\end{code}

\begin{code}{date/days-range-separator}
  \begin{syntax}
    \key{date/days-range-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{day_1} and \meta{day_2} in a day range to
  \meta{token list}.
  The initial value is | to |.
\end{code}

\subsubsection{Separating the months}
\label{subsubsec:sepmonths}

\begin{code}{date/months-separator}
  \begin{syntax}
    \key{date/months-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{months}---except the last
  two---to \meta{token list} if \meta{months} contains three or more items.
  The initial value is |, |.
\end{code}

\begin{code}{date/months-pair-separator}
  \begin{syntax}
    \key{date/months-pair-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between each item in \meta{months} to \meta{token list} if
  \meta{months} contains exactly two items.
  The initial value is | and |.
\end{code}

\begin{code}{date/months-final-separator}
  \begin{syntax}
    \key{date/months-final-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between the last two items in \meta{months} to
  \meta{token list} if \meta{months} contains three or more items.
  The initial value is |, and |.
\end{code}

\begin{code}{date/months-range-separator}
  \begin{syntax}
    \key{date/months-range-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between \meta{month_1} and \meta{month_2} in a month range
  to \meta{token list}.
  The initial value is | to |.
\end{code}

\subsubsection{Printing the era}
\label{subsubsec:printera}

\begin{code}{date/era}
  \begin{syntax}
    \key{date/era=A.D.|AD|C.E.|CE|integer}
  \end{syntax}
  Choice key that sets the format in which the era is printed.
  The initial value is |CE|.
  \input{clockdate-input-era.tex}

  |integer| prints the year as an integer which may be positive, zero, or
  negative.

  If the calendar date ends with an era that ends with a period, then the
  trailing era will not print an extra period if the next token after
  \cs{calendardate} is a period.
  If the era prints the period, it is followed by \cs{@} because the sentence
  does not end here.
\end{code}

\begin{code}{date/era-threshold}
  \begin{syntax}
    \key{date/era-threshold=\meta{integer}}
  \end{syntax}
  The AD and CE are printed only if \meta{year} is less than \meta{integer}.
  The initial value is \num{100}.
\end{code}

\begin{code}{date/era-separator}
  \begin{syntax}
    \key{date/era-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between the year and the era to \meta{token list}.
  The initial value is \cs{nobreakspace}.
\end{code}

\begin{code}{date/ad-first}
  \begin{syntax}
    \key{date/ad-first=true|false}
  \end{syntax}
  Boolean key that sets if the AD is printed before the year.
  The initial value is |true|.
\end{code}

\begin{code}{date/ad-first-separator}
  \begin{syntax}
    \key{date/ad-first-separator=\meta{token list}}
  \end{syntax}
  Sets the separator between the year and the AD to \meta{token list} if
  \key{date/ad-first=true}.
  The initial value is \cs{nobreakspace}.
\end{code}

\begin{code}{date/positive-year-prefix}
  \begin{syntax}
    \key{date/positive-year-prefix=\meta{token list}}
  \end{syntax}
  Prepends \meta{token list} to the printed year if \key{date/era=integer} and
  the year is positive.
  The initial value is \meta{empty}.
\end{code}

\begin{code}{date/negative-year-prefix}
  \begin{syntax}
    \key{date/negative-year-prefix=\meta{token list}}
  \end{syntax}
  Prepends \meta{token list} to the printed year if \key{date/era=integer} and
  the year is negative.
  The initial value is \cs{textminus}.
\end{code}

\begin{code}{date/zero-year-prefix}
  \begin{syntax}
    \key{date/zero-year-prefix=\meta{token list}}
  \end{syntax}
  Prepends \meta{token list} to the printed year if \key{date/era=integer} and
  the year is zero.
  The initial value is \meta{empty}.
\end{code}


\section{Programming}
\label{sec:programming}

This section documents the programming support provided by the \pkg{clockdate}
package.

\subsection{Hooks}
\label{subsec:hooks}

This subsection documents the hooks provided by the \pkg{clockdate} package.

\begin{code}
  {
    cmd/clock/after,
    cmd/calendardate/after,
    cmd/Calendardate/after,
    cmd/date/after,
    cmd/Date/after
  }
  These generic |cmd| hooks are disabled because they would prevent the command
  from testing if the next token is a period (See \S\ref{subsubsec:printampm},
  \S\ref{subsubsec:printymd}, and \S\ref{subsubsec:printera}).
  \hook{cmd/date/after} and \hook{cmd/Date/after} are disabled only if the key
  \key{overwrite-date} is set (\S\ref{sec:keys}).
\end{code}

\begin{code}{clockdate/clock}
  Hook used in \cs{clock} after parsing its input and setting all the variables
  in \S\ref{subsec:varclock}, but before printing the time of day.
\end{code}

\begin{code}{clockdate/date}
  Hook used in \cs{calendardate} after parsing its input and setting all the
  variables in \S\ref{subsec:vardate}, but before printing the calendar date.
\end{code}

\subsection{Precompiling the keys}
\label{subsec:save}

\begin{code}{\clockdate_clock_keys_save:nn}
  \begin{syntax}
    \cs{clockdate_clock_keys_save:nn} \marg{string} \marg{key-value list}
  \end{syntax}
  Precompiles the keys in \meta{key-value list} in path |clock| for use when
  setting the key \key{clock/load=\meta{string}} (\S\ref{subsec:clock}).
\end{code}

\begin{code}{\clockdate_date_keys_save:nn}
  \begin{syntax}
    \cs{clockdate_date_keys_save:nn} \marg{string} \marg{key-value list}
  \end{syntax}
  Precompiles the keys in \meta{key-value list} in path |date| for use when
  setting the key \key{date/load=\meta{string}} (\S\ref{subsec:date}).
\end{code}

\subsection{Variables set by \cs{clockdatesetup}}
\label{subsec:varsetup}

This subsection documents the variables that are set by \cs{clockdatesetup}
(\S\ref{sec:cmds}).
They are read-only and should not be modified directly.

\begin{code}{\l_clockdate_clock_clock_str}
  The value of the key \key{clock/clock} (\S\ref{subsec:clock}).
\end{code}

\begin{code}
  {
    \l_clockdate_clock_hour_format_str,
    \l_clockdate_clock_minute_format_str,
    \l_clockdate_clock_second_format_str
  }
  The values of the keys documented in \S\ref{subsubsec:printhms}.
\end{code}

\begin{code}
  {
    \l_clockdate_clock_hour_minute_separator_tl,
    \l_clockdate_clock_minute_second_separator_tl
  }
  The values of the keys documented in \S\ref{subsubsec:sephms}.
\end{code}

\begin{code}
  {
    \l_clockdate_clock_ampm_format_str,
    \l_clockdate_clock_ampm_separator_tl,
    \l_clockdate_clock_ampm_omit_range_bool,
    \l_clockdate_clock_ampm_omit_list_bool
  }
  The values of the keys documented in \S\ref{subsubsec:printampm}.
\end{code}

\begin{code}
  {
    \l_clockdate_clock_clocks_separator_tl,
    \l_clockdate_clock_clocks_pair_separator_tl,
    \l_clockdate_clock_clocks_final_separator_tl,
    \l_clockdate_clock_clocks_range_separator_tl
  }
  The value of the keys documented in \S\ref{subsubsec:sepclocks}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_priority_str,
    \l_clockdate_date_order_str,
    \l_clockdate_date_capitalize_first_bool
  }
  The value of the keys documented at the start of \S\ref{subsec:date}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_year_format_str,
    \l_clockdate_date_month_format_str,
    \l_clockdate_date_day_format_str,
    \l_clockdate_date_day_ordinal_bool
  }
  The values of the keys documented in \S\ref{subsubsec:printymd}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_year_month_separator_tl,
    \l_clockdate_date_month_day_separator_tl,
    \l_clockdate_date_year_day_separator_tl
  }
  The values of the keys documented in \S\ref{subsubsec:sepymd}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_days_separator_tl,
    \l_clockdate_date_days_pair_separator_tl,
    \l_clockdate_date_days_final_separator_tl,
    \l_clockdate_date_days_range_separator_tl
  }
  The values of the keys documented in \S\ref{subsubsec:sepdays}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_months_separator_tl,
    \l_clockdate_date_months_pair_separator_tl,
    \l_clockdate_date_months_final_separator_tl,
    \l_clockdate_date_months_range_separator_tl
  }
  The values of the keys documented in \S\ref{subsubsec:sepmonths}.
\end{code}

\begin{code}
  {
    \l_clockdate_date_era_format_str,
    \l_clockdate_date_era_threshold_int,
    \l_clockdate_date_era_separator_tl,
    \l_clockdate_date_ad_first_bool,
    \l_clockdate_date_ad_first_separator_tl,
    \l_clockdate_date_positive_year_prefix_tl,
    \l_clockdate_date_negative_year_prefix_tl,
    \l_clockdate_date_zero_year_prefix_tl
  }
  The values of the keys documented in \S\ref{subsubsec:printera}.
\end{code}

\subsection{Variables set in \cs{clock}}
\label{subsec:varclock}

This subsection documents the variables that are set within \cs{clock}.
Outside of \cs{clock}, they do not have defined behavior.

\begin{code}{\l_clockdate_clock_clocks_str}
  The value of \meta{clock(s)}.
  The value takes the same form as the \meta{clock(s)} as explained for
  \cs{clock}.
  The \meta{hour}, \meta{minute}, and \meta{second} are separated by a colon.
\end{code}

\subsection{Variables set in \cs{calendardate}}
\label{subsec:vardate}

This subsection documents the variables that are set within \cs{calendardate}.
Outside of \cs{calendardate}, they do not have defined behavior.

\begin{code}{\l_clockdate_date_year_str}
  The value of \meta{year}.
  The value is \meta{empty} if \meta{year} is omitted.
\end{code}

\begin{code}{\l_clockdate_date_month_str}
  The value of \meta{month(s)}.
  The value takes the same form as the \meta{month(s)} as explained for
  \cs{calendardate}.
  The value is an \meta{integer} if exactly one month is printed.
  The value is \meta{empty} if no months are printed.
\end{code}

\begin{code}{\l_clockdate_date_day_str}
  The value of \meta{day(s)}.
  The value takes the same form as the \meta{day(s)} as explained for
  \cs{calendardate}.
  The value is an \meta{integer} if exactly one day is printed.
  The value is \meta{empty} if \meta{day(s)} is omitted.
\end{code}

\subsection{Functions used in \cs{clock}}
\label{subsec:expl3clock}

This subsection documents the public functions used in \cs{clock}.
You may redefine them, but they must be |protected| and have the same parameter
text.
The initial definitions are in \file{clockdate.sty}.

\begin{code}{\clockdate_clock_print_hour:n}
  \begin{syntax}
    \cs{clockdate_clock_print_hour:n} \marg{hour}
  \end{syntax}
  Function used in \cs{clock} that prints the hour.
  |#1| represents the value of \meta{hour}.
\end{code}

\begin{code}{\clockdate_clock_print_minute:n}
  \begin{syntax}
    \cs{clockdate_clock_print_minute:n} \marg{minute}
  \end{syntax}
  Function used in \cs{clock} that prints the minute.
  |#1| represents the value of \meta{minute}.
\end{code}

\begin{code}{\clockdate_clock_print_second:n}
  \begin{syntax}
    \cs{clockdate_clock_print_second:n} \marg{second}
  \end{syntax}
  Function used in \cs{clock} that prints the second.
  |#1| represents the value of \meta{second}.
\end{code}

\begin{code}{\clockdate_clock_print_am:, \clockdate_clock_print_pm:}
  Functions used in \cs{clock} that print the am and pm.
\end{code}

\subsection{Functions and variables used in \cs{calendardate}}
\label{subsec:expl3date}

This subsection documents the public functions and variables used in
\cs{calendardate}.
You may redefine the public functions, but they must be |protected| and have
the same parameter text.
The initial definitions are in \file{clockdate.sty}.

\begin{code}{\clockdate_date_print_year:n}
  \begin{syntax}
    \cs{clockdate_date_print_year:n} \marg{year}
  \end{syntax}
  Function used in \cs{calendardate} that prints the year, including the era.
  |#1| represents the value of \meta{year}.
\end{code}

\begin{code}{\clockdate_date_print_month:n}
  \begin{syntax}
    \cs{clockdate_date_print_month:n} \marg{month}
  \end{syntax}
  Function used in \cs{calendardate} that prints the month.
  |#1| represents the value of \meta{month}.
\end{code}

\begin{code}
  {\l_clockdate_date_months_mmmm_seq, \l_clockdate_date_months_mmm_seq}
  Variables used in \cs{clockdate_date_print_month:n} that store the months
  used if the value of the key \key{date/month} is |mmmm| or |mmm|.
  The initial values are displayed in in Table \ref{table:months-seq}.
\end{code}

\input{clockdate-input-months-seq.tex}

\begin{code}{\l_clockdate_date_if_capitalized_bool}
  Boolean that may be used in \cs{clockdate_date_print_month:n} to capitalize
  the month if it is printed at the start of \cs{calendardate}.
  This variable does not have defined behavior outside of
  \cs{clockdate_date_print_month:n}.
  This variable may be used in \cs{l_clockdate_date_months_mmm_seq} and
  \cs{l_clockdate_date_months_mmmm_seq}.
\end{code}

\begin{code}{\clockdate_date_print_day:n}
  \begin{syntax}
    \cs{clockdate_date_print_day:n} \marg{day}
  \end{syntax}
  Function used in \cs{calendardate} that prints the day.
  |#1| represents the value of \meta{day}.
\end{code}

\begin{code}{\clockdate_date_day_ordinal_format:n}
  \begin{syntax}
    \cs{clockdate_date_day_ordinal_format:n} \marg{day}
  \end{syntax}
  Function used in \cs{clockdate_date_print_day:n} that sets the format in
  which the suffix of the ordinal day is printed.
  |#1| represents the value of \meta{day}.
  The initial definition is:
  \begin{verbatim}
\cs_new_protected:Npn \clockdate_date_day_ordinal_format:n #1
  {
    \textsuperscript
      {
        \int_case:nnF {#1}
          {
            {  1 } { st } {  2 } { nd } {  3 } { rd }
            { 21 } { st } { 22 } { nd } { 23 } { rd }
            { 31 } { st }
          }
          { th }
      }
  }
  \end{verbatim}
\end{code}

\subsection{The extra period problem}
\label{subsec:extraperiod}

\begin{code}{\clockdate_end_period:}
  Function used in \cs{clock} and \cs{calendardate} to avoid mistakenly
  printing two consecutive periods if the next token after \cs{clock} or
  \cs{calendardate} is a period.

  This function is used in:
  \begin{itemize}
    \item \cs{clockdate_clock_print_am:}, \cs{clockdate_clock_print_pm:}
    \item
    \cs{clockdate_date_print_month:n}, \cs{l_clockdate_date_months_mmm_seq}
    \item \cs{clockdate_date_print_year:n}
  \end{itemize}

  Prints a period unless both of the following are true:
  \begin{itemize}
    \item
    This function is used at the very end of \cs{clock} or \cs{calendardate},
    followed by zero or more tokens with the same meaning as \cs{group_end:}.
    \item The next token after \cs{clock} or \cs{calendardate} is a period.
  \end{itemize}

  The gobbling and replacement of \cs{group_end:} tokens or equivalent ensures
  that the period has the same formatting as the preceding text.
  If \cs{clockdate_end_period:} prints the period, it is followed by \cs{@}
  because the sentence does not end here.
\end{code}


\section{References}
\label{sec:ref}

The \pkg{clockdate} package uses some ideas from the \pkg{datetime2} and
\pkg{siunitx} packages.

\listheading
  {
    The keys in \S\ref{subsubsec:sephms} resemble the following \pkg{datetime2}
    options:
  }
\begin{itemize}
  \item \key{hourminsep}
  \item \key{minsecsep}
  \item \key{timesep}
\end{itemize}

\listheading
  {
    The keys in \S\ref{subsubsec:sepymd} resemble the following \pkg{datetime2}
    options:
  }
\begin{itemize}
  \item \key{yearmonthsep}
  \item \key{monthdaysep}
  \item \key{dayyearsep}
  \item \key{datesep}
\end{itemize}

\listheading
  {
    The keys in \S\ref{subsubsec:sepclocks}, \S\ref{subsubsec:sepdays}, and
    \S\ref{subsubsec:sepmonths} use a similar naming convention to the
    following \pkg{siunitx} options:
  }
\begin{itemize}
  \item \key{list-separator}
  \item \key{list-pair-separator}
  \item \key{list-final-separator}
\end{itemize}


\end{document}
